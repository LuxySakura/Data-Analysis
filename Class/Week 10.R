# Outcome: Score
# Predictors: Gender, Education

# lm(Score ~ Gender + Education)
# Intercept: score for the baseline

# log(charges) = b0 + b1*smoker
# charges = exp(b0)*exp(b1*smoker) = exp(b0)*exp(b1)^smokers

# ANOVA
# Uses the F distribution, which the